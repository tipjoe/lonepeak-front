/**
 * This is the location ID and street.
 *
 * Used in the registration form to limit street selection to known ones
 * within the neighborhood.
 */
export interface LocationStreet {
  id: number;
  value: string;
}
